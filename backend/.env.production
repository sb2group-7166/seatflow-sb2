# Server Configuration
PORT=3000
NODE_ENV=production

# MongoDB Configuration
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/seatflow?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your-production-secret-key
JWT_EXPIRATION=1d

# CORS Configuration
CORS_ORIGIN=https://your-frontend-domain.com

# Rate Limiting
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX=100

# File Storage
BACKUP_DIR=/data/backups
LOG_DIR=/data/logs 